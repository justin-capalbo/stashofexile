type Query {
    hello: String!
    getTabs(input: GetTabsInput!): [Tab!]
}

input GetTabsInput {
    creds: AccountInfo!
}

input AccountInfo {
    poeSessId: String!
    accountName: String!
    league: String!
}

type Tab {
    name: String!
    index: Int!
    type: String!
    color: Color!
}

type Color {
    r: Int!,
    g: Int!,
    b: Int!
}
