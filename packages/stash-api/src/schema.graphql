type Query {
    hello: String!
    getTabs(poeInfo: PoeInfo!): [Tab!]
}

input PoeInfo {
    poeSessId: String!
    accountName: String!
    league: String!
}

type Tab {
    name: String!
    index: Int!
    type: String!
    color: Color!
}

type Color {
    r: Int!,
    g: Int!,
    b: Int!
}
